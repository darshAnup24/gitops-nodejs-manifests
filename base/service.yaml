apiVersion: v1
kind: Service
metadata:
  name: gitops-service
  namespace: gitops-app
  labels:
    app: gitops-app
spec:
  type: NodePort
  selector:
    app: gitops-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
